apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "adminapp.fullname" . }}-test-connection"
  labels:
    {{- include "adminapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "adminapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
